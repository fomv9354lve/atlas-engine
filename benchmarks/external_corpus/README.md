# External-validation corpus

Atlas evaluated on circuits that **others** published or assumed hard — **not** our
self-generated corpus. The goal is to test the routing verdict against the field's own
"this needs a QPU" intuition: quantum-advantage circuits plus large published
algorithm / benchmark circuits.

This directory is fully reproducible. Each circuit ships as OpenQASM under `qasm/`
(or is regenerated by the runner scripts), and the verdicts are in the four
`results.json` files. No secrets, no tokens — only circuits and their routing output.

## What ran

| File | Tier | Circuits |
|------|------|----------|
| `results.json` | Tier 1 — advantage + published algorithm circuits | 18 |
| `batch2_published/results.json` | QASMBench + MQT-Bench published algorithm circuits | 69 |
| `tier2_vqe/results.json` | Tier 2 — VQE / UCCSD ansätze (HEA + chemistry) | 12 |
| `tier2_qaoa_trotter/results.json` | Tier 2 — QAOA + Trotter dynamics | 16 |

**Consolidated (dedup by circuit name across the four files): 115 unique circuits.**

Re-run 2026-06 through the **fixed engine** (per-estimator wall-clock budgets + graceful
abstain — see `RERUN_SUMMARY.md`). Current tally:

- **108 routed to a classical tier** (CPU 102 · TENSOR 4 · HPC_FIRST 2).
- **7 escalated** — out of classical reach: `sycamore_rcs_53q_depth8`,
  `sycamore_rcs_53q_depth20`, `kicked_ising_127q_hard_5steps`, `qb_QV_n100`,
  `qb_qft_n160`, `qb_bwt_n37`, `qb_square_root_n45`.
- **0 timed out** — every circuit resolves.

> **Note on the earlier figures.** A prior run reported 14 timeouts (and an even older
> partial run, 46 circuits). Those 14 were an **engine estimator-budget bug** (certain
> small-n circuits stalled a single estimator), **not** circuit hardness. With
> per-estimator budgets now in the engine, all 14 resolve — **10 to a classical tier,
> 4 to genuine ESCALATE** (`qb_QV_n100`, `qb_qft_n160`, `qb_bwt_n37`, `qb_square_root_n45`,
> re-probed at a 180 s budget). If you see 98/3/14 or 46/41/3/2 cited anywhere, it is
> stale — **108 / 7 / 0** is current, and is what the `results.json` files contain.

The honest punch line: Atlas does **not** rubber-stamp everything classical. It
**escalated the genuine advantage frontier** (Sycamore RCS at supremacy depth; the
*hard* IBM-utility kicked-Ising point; large/high-entanglement circuits at n=37–160),
while the *easy / Clifford* kicked-Ising control point routed CLASSICAL — matching the
field's own finding that the easy regime is classically reachable and the hard regime is
not. And `multiplier_n45` has T-count 2,646 yet routes CPU at MPS bond 1: **magic ≠ hardness**.

"Classical tier" does **not** mean "laptop-trivial" — several are TENSOR/HPC-reachable.
The honest meaning is that methods the field assumed were QPU-only (large tensor-network
/ statevector / stabilizer runs) reach them.

## Sources of the circuits

- **Sycamore random circuit sampling (RCS)** — the 2019 "quantum supremacy" family.
  Arute et al., *Nature* 574, 505 (2019). Later classically reproduced post-hoc:
  Pan & Zhang, *PRL* 129, 090502 (2022); Liu et al., Gordon Bell 2021. Atlas escalates
  it (correct — genuinely high-treewidth at depth).
- **IBM 127-qubit "utility" kicked-Ising** — Kim et al., *Nature* 618, 500 (2023).
  Classically reproduced by Tindall, Fishman, Stoudenmire & Sels, *PRX Quantum* 5,
  020332 (2024); Begušić & Chan (2024); and others via tensor networks. We include
  three points: the *hard* point (escalates), a *Clifford* control point (routes
  classical, Stim poly-time, Aaronson–Gottesman 2004), and a 20-step variant.
- **Algorithm / benchmark circuits** — QASMBench-style and MQT-Bench-style families:
  adder, multiplier, GHZ-127, cat, Bernstein–Vazirani, QFT, Quantum Volume, HHL,
  Shor/factoring, quantum walks, kNN/DNN (QML), QuGAN. Constructed/obtained as
  representative instances at the stated sizes.
- **VQE / UCCSD** (`tier2_vqe/`) — hardware-efficient ansätze (linear + full
  entanglement) and chemistry UCCSD-style circuits (H2, LiH, BeH2).
- **QAOA + Trotter** (`tier2_qaoa_trotter/`) — QAOA-MaxCut (line + dense) and Trotterized
  TFIM / Heisenberg dynamics on line and grid topologies.

## Reproduce

```bash
# Tier 1 (advantage + algorithm circuits)
python3 run_corpus.py        # main harness; run_phase2.py for the advantage points

# Tier 2
python3 tier2_vqe/gen_and_run.py
python3 tier2_qaoa_trotter/run_corpus.py
```

Engine used: `cost_atlas` + `route_adjudicator` (the same REAL estimators as the main
corpus — magic / MPS-bond / treewidth / stabilizer, min over methods). This was an
offline research harness; nothing was deployed by it.

## Honest caveats

- These are constructed/obtained representative circuits, not a harvested production
  trace. They are **size-faithful** to the cited works but **not bit-identical** to every
  original — a probe against the field's intuition, not an exact reproduction.
- **Escalate ≠ solve, and ESCALATE at scale rests on bounds, not exact measurement.**
  In the genuinely-hard regime (the BQP≠BPP wall) Atlas gives an **indication, not a
  proof**: it did **not** simulate or certify Sycamore / kicked-Ising — it flagged them
  as beyond classical reach. At 53–127 qubits the treewidth is a cotengra **upper bound**
  (`exact=false`) and the MPS bond is **truncated** (`trunc=true`) — inevitable at that
  scale. So an ESCALATE verdict is a **negative result** (Atlas did not wrongly call them
  classical), **not** a certified hardness proof. This is by design — Atlas triages which
  side of the frontier a circuit is on; it does not move the frontier.
- "Classical tier" ≠ "trivial" — several are TENSOR/HPC-reachable, not laptop-trivial.
- Numbers here do not generalize beyond this named set; this is a **per-circuit**
  validation probe against the field's intuition, **not** a universal "X %" claim.
